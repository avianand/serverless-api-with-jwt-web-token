# serverless-api-with-jwt-web-token-mongoDB
 Serverless REST API with a few endpoints, and an authorizer function. This authorizer will act as the middleware for authorizing access to your resources.
